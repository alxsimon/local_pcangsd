[tool.poetry]
name = "local-pcangsd"
version = "0.1.0"
description = ""
authors = ["Alexis Simon <alexis.simon@normalesup.org>"]
readme = "README.md"
packages = [{include = "local_pcangsd"}]

[tool.poetry.dependencies]
python = "^3.10"
numpy = "^1.24.3"
xarray = "^2023.5.0"
sgkit = "^0.7.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
